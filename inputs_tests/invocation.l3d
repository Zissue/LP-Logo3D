// Invocation test

PROC f(a,b) IS

    forward(mida)
    costats := 3
    left(360 / costats)

    cercle(1, 12)
    up(5)
    espiral(cercles - 1)

    espiral(5)

    euclides(valor1, valor2)

    
    color(0.2, 0.2, 1)
    home()

    show()
    hide()


    forward(mida)
    backward(32)
    down(90)
    up(ol)
    left(0)
    right(ca)

    escriu(numero, 2)
END

PROC main() IS
    f(0.2,val1*9)
END
